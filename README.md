interface for quick analysis of phase/amplitude estimation.

runs on electron. Uses python for data analysis.

requirements:
  npm:
    electron
    jquery
    bootstrap
    pyshell
    plotly
    scale-number-range
  python:
    numpy
    hdf5
    scipy
    json
    sys